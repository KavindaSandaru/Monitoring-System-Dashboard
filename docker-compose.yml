services:

  backend:
    build: ./backend/monitoring-system
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend/monitoring-dashboard
    ports:
      - "3000:3000"
    depends_on:
      - backend